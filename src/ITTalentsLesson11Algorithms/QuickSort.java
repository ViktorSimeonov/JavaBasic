package ITTalentsLesson11Algorithms;

import java.lang.reflect.Array;
import java.util.Arrays;

public class QuickSort {
    public static void main(String[] args) {
        int[] arr = {1, 5, 3, 9, 2, 8, 4};

      /*  sort(arr);
        System.out.println(partition(arr));
        System.out.println(Arrays.toString(arr));
    }
    static int partition(int[] arr,int start, int end) {
        int pivotIndex = arr[arr.length - 1];
        int pivot=arr[pivotIndex];
        // check all elements and compare with pivot
        //if less -> swat with leftomst
        int leftMostIndex = 0;
        for (int i = 0; i < arr.length - 1; i++) {
            if (arr[i] < pivot) {
                int temp = arr[i];
                arr[i] = arr[leftMostIndex];
                arr[leftMostIndex] = temp;
                leftMostIndex++;
            }
        }
        int temp = arr[pivotIndex];
        arr[pivotIndex]=arr[leftMostIndex];
        arr[leftMostIndex]=temp;
        return leftMostIndex;
    }

    static void sort(int[] arr) {
*/

    }
}
